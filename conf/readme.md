# 在线使用
> 本 yaml 文件可以使用 /overwrite/overwrite.conf 直接在 openclash 覆写模块中一键拉取并定期更新；
> 也可以使用订阅链接方式拉取，不需要设置 UA，推荐使用本方法，记得改订阅名，方便/overwrite/overwrite_local.conf 手动覆写

# 本地使用
> 也可以下载后上传到 openclash 中结合 /overwrite/overwrite_local.conf 手动覆写
